# Jumunpangpang OMS 프로젝트 개발 명세서

이 문서는 **'주문팡팡(Jumunpangpang) AI 주문처리 시스템'** 프론트엔드 개발을 위한 핵심 명세서입니다.

---

## 제공된 리소스 (Input)

1. **기획서 (Markdown)**: `docs/` 폴더 내의 파일들은 비즈니스 로직, 데이터 정책, 예외 처리의 **절대 기준(Single Source of Truth)**입니다.
   - `대시보드_기획서.md`: 매출 차트, 캐시백, 통합 알림
   - `주문관리_기획서.md`: **[핵심]** 주문 수집, 상태 관리, 배송 추적, 클레임 처리
   - `문의관리_기획서.md`: CS 통합 관리 (마켓별 제약 사항 포함)
   - `내정보_기획서.md`: 멤버십 결제, 마켓 API 연동, 알림톡 설정

2. **스크린샷 (Images)**: `docs/screenshots/` 폴더 내 이미지들은 **레이아웃과 UI 디자인 레퍼런스**입니다.

---

## 개발 3대 원칙 (Critical Rules)

### 원칙 1: 기획서 우선
기획서 내용과 스크린샷이 다르게 보인다면? **무조건 기획서 텍스트를 따르십시오.** 스크린샷은 참고용이며, 최신 비즈니스 로직은 오직 마크다운 기획서에만 반영되어 있습니다.

### 원칙 2: 실제 서비스 참조 (Live Verification)
정적인 문서만으로 인터랙션이 이해가 안 간다면? **실제 서비스에 로그인해서 직접 눌러보십시오.**

- **접속 URL**: `https://oms.jumunpangpang.com/`
- **테스트 계정**: (환경변수 또는 팀 내 공유)

### 원칙 3: 핵심 로직 완벽 구현
UI만 구현하는 것이 아니라, 기획서에 명시된 정책대로 백엔드 로직/예외 처리를 수행해야 합니다.

---

## 필수 구현 핵심 로직 (Must-Implement Features)

### 1. 송장 실시간 동기화 제약 (주문관리)
- **쿠팡/ESM**: 가송장 -> 실송장 **자동 업데이트** 구현
- **스마트스토어/11번가**: API 제약으로 **자동 업데이트 불가**. "직접 수정해주세요" **[작업 필요 UI]** 노출 필수
- **11번가 특이사항**: 가송장 입력 시 '직접전달' 모드로 전송

### 2. 2단계 반품 프로세스 (클레임)
반품은 버튼 하나로 끝나지 않습니다:
1. **[1단계]**: 소싱처 주소 확보
2. **[2단계]**: 배대지 반품 신청

> 주소가 없으면 신청서 작성 불가하도록 UX 강제

### 3. 통관부호(PCCC) 검증 로직 (주문관리)
- **미수집 시**: 빨간색 텍스트 경고 + **[통관부호 확인]** 파란색 버튼 활성화
- **자동화**: 백그라운드에서 알림톡 최대 3회 자동 발송

### 4. 문의 수집 예외 처리 (문의관리)
- **ESM/11번가**: 문의 수집 API 없음
- 해당 탭 진입 시 "지원하지 않는 마켓입니다" 문구 또는 외부 마켓 링크 제공

---

## 기술 스택 (Tech Stack)

| 영역 | 기술 |
|------|------|
| **Framework** | Next.js 16 (App Router), React 19, TypeScript |
| **State Management** | React Query (Server State), Zustand (Client State) |
| **Styling** | Tailwind CSS 4, Shadcn UI |
| **Chart** | Recharts |
| **Icons** | Lucide React |

---

## 목표 (Goal)

위 명세를 준수하여, 사용자가 **"기획서 그대로 나왔네요!"**라고 말할 수 있는 완벽한 프론트엔드 결과물을 생성하십시오.
